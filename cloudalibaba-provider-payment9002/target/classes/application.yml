server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
#        server-addr: localhost:8848
        server-addr: localhost:1111
management:
  endpoints:
    web:
      exposure:
        include: '*'